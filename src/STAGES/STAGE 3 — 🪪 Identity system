We are now implementing the HMS identity layer.

Before modifying anything, inspect the existing ID strategy.

Requirement:

Every patient must have a permanent unique human-readable Patient ID.

Example:

AMC-PT-000001

Every staff member should have a permanent Staff ID.

Example:

AMC-ST-000001

Every appointment should have an Appointment ID.

Example:

AMC-APT-000001

Do not blindly use these formats. Determine the safest implementation based on the existing schema.

The existing database primary keys/UUIDs must remain intact unless there is a compelling reason otherwise.

Patient IDs must be generated server-side.

The browser must never supply the authoritative Patient ID.

Most importantly:

ALL patient creation paths must use the same authoritative service.

Public /register
Admin registration
Reception registration

must eventually result in:

Patient record
+
Patient ID
+
audit event

Do not duplicate patient creation logic.

Before implementing, report whether schema changes are required.

Do not run destructive database commands.

Do not deploy.

After implementation:

npx prisma validate
npx prisma generate
npm run build

STOP.